# Python Rule

For Python changes:

- Keep scripts explicit and readable
- Validate configuration early
- Log meaningful operational context
- Avoid logging secrets or sensitive data
- Prefer small functions with clear responsibility
- Add dry-run mode for integration/data sync scripts when useful
- Keep data serialization rules documented
