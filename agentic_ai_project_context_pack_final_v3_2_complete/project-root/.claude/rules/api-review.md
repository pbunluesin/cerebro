# API Review Rule

Before changing API behavior:

- Read `docs/api-contract.md`
- Read `docs/integration-rules.md`
- Preserve request/response compatibility unless approved
- Validate auth behavior
- Document error handling
- Update sample payloads if the contract changes
