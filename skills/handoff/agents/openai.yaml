interface:
  display_name: "Project Handoff"
  short_description: "Preserve verified context across sessions"
  default_prompt: "Use $handoff continuation to create a concise verified continuation checkpoint for this project."

policy:
  allow_implicit_invocation: false
